    <tr>
      <td><%= task.name %></td>
      <td><%= TasksHelper::STATUS_MAP[task.status] %></td>
      <td><%= task.type.name%></td>

      <td><%= task.user.staff.name if task.user %></td>
      <td><%= task.updated_at.strftime("%Y-%m-%d %H:%M:%S") %></td>
      <td class="link-container">
           <%= link_to "編輯", edit_task_path(task), class: "custom-link" %>
           <%= button_to "受理", accept_task_path(task), {method: :patch, form: {data: {turbo: true}}, class: "custom-link"} %>
           <%= button_to "完成", finish_task_path(task), {method: :patch, form: {data: {turbo: true}}, class: "custom-link"} %>

      </td>
    </tr>